app = "casapin-backend"
primary_region = "cdg"

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "8080"

[[services]]
  protocol = "tcp"
  internal_port = 8080
  processes = ["app"]

  [[services.ports]]
    port = 80
    handlers = ["http"]

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]

  # Health checks HTTP -> /api/health
  # IMPORTANTE: Usar el puerto interno (8080) y protocolo HTTP explícito
  [[services.http_checks]]
    method = "GET"
    path = "/api/health"
    interval = "15s"
    timeout = "10s"
    grace_period = "180s"
    protocol = "http"
    port = 8080

  # TCP check por si arranca más lento
  [[services.tcp_checks]]
    interval = "10s"
    timeout = "5s"
    grace_period = "180s"


