# Copia este archivo a .env y completa los valores

# MongoDB
MONGODB_URI=mongodb://localhost:27017/casapin
# O para MongoDB Atlas:
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/casapin?retryWrites=true&w=majority

# Server
PORT=8080
NODE_ENV=development

# CORS - IMPORTANTE: En producción, especifica tu dominio exacto
# Ejemplo: https://casapin.netlify.app
# Puedes especificar múltiples orígenes separados por comas
CORS_ORIGIN=http://localhost:5173

# Admin - Token para endpoints administrativos
# Genera uno seguro: openssl rand -hex 32
ADMIN_TOKEN=your-secret-admin-token-here

# Google Places API (opcional, para actualizar ratings)
GOOGLE_PLACES_API_KEY=your-google-places-api-key

# Email/SMTP (opcional, para formulario de contacto)
CONTACT_TO=tu-email@ejemplo.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-app-password

